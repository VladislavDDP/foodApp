import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  iconContainer: {
    position: 'relative',
    padding: 5,
  },
  qtyContainer: {
    position: 'absolute',
    right: 0,
    borderRadius: 50,
    backgroundColor: 'red',
    paddingHorizontal: 5,
  },
  qtyText: {
    color: '#fff',
    textAlign: 'center',
  },
});
