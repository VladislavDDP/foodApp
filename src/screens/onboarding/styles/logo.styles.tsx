import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  logoContainer: {
    borderRadius: 100,
    width: 70,
    height: 70,
    backgroundColor: '#fff',
    borderColor: '#fff',
    borderWidth: 10,
  },
});
