import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  deliveryMethodContainer: {
    borderRadius: 20,
    padding: 20,
    backgroundColor: '#fff',
    marginTop: 20,
  },
  sectionTitle: {
    fontSize: 17,
    fontWeight: '700',
  },
});
