import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  refreshButton: {
    borderRadius: 30,
    backgroundColor: '#FA4A0C',
    paddingVertical: 20,
    width: '100%',
    marginVertical: 20,
  },
  btnText: {
    textAlign: 'center',
    color: '#fff',
    fontWeight: '600',
  },
});
