import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  button: {
    bottom: 30,
    borderRadius: 30,
    paddingVertical: 25,
    position: 'absolute',
    zIndex: 6,
    right: 0,
    left: 0,
    marginHorizontal: 30,
  },
  buttonText: {
    textAlign: 'center',
  },
});
