import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  button: {
    bottom: 20,
    borderRadius: 30,
    paddingVertical: 25,
    position: 'absolute',
    zIndex: 10,
    right: 0,
    left: 0,
    marginHorizontal: 30,
  },
  buttonText: {
    textAlign: 'center',
  },
});
