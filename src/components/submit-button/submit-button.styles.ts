import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#FF460A',
    marginHorizontal: 50,
    marginTop: 15,
    borderRadius: 10,
  },
  text: {
    color: '#fff',
    textAlign: 'center',
    padding: 10,
  },
});
