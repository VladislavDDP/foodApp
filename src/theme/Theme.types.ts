export interface Theme {
  colorScheme: {
    primary: string;
    primaryLight: string;
    primaryDark: string;
    text: string;
    secondary: string;
    shadow: string;
    accent: string;
  };
}
